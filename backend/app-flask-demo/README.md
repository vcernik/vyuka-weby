# Ukázková aplikace – backend pomocí frameworku Flask (Python)

## Spuštění
- Instalace Flasku: `pip install Flask`
- Spuštění v debug módu `flask run --debug`

Aplikace běží na `http://127.0.0.1:5000`.